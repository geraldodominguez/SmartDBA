###############################################################################
# COMPORTAMENTO PADRÃO (GLOBAL)
###############################################################################
# Tenta detectar automaticamente se é texto ou binário.
# No Windows, converte LF para CRLF ao baixar.
* text=auto

###############################################################################
# SCRIPTS DE BANCO DE DADOS (O CORE DO SEU PROJETO)
###############################################################################
# Garante que o Git trate SQL e PL/SQL sempre como texto
*.sql text
*.plsql text
*.tql text

###############################################################################
# SCRIPTS DE AUTOMAÇÃO (CRÍTICO PARA CROSS-PLATFORM)
###############################################################################
# Scripts Linux/Oracle DEVEM ter final de linha LF, senão quebram no servidor.
*.sh text eol=lf
*.bash text eol=lf
*.command text eol=lf

# Scripts Windows/SQL Server DEVEM ter final de linha CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

###############################################################################
# ARQUIVOS BINÁRIOS (NÃO TENTAR LER LINHA POR LINHA)
###############################################################################
# Imagens (Descomentei as linhas do seu arquivo original)
*.jpg binary
*.png binary
*.gif binary
*.ico binary

# Arquivos de Banco de Dados e Backups (Essencial para não travar o Git)
*.bak binary
*.bacpac binary
*.dacpac binary
*.dmp binary
*.mdf binary
*.ldf binary
*.ndf binary

# Arquivos compactados e executáveis
*.zip binary
*.7z binary
*.exe binary
*.dll binary

###############################################################################
# ARQUIVOS DE PROJETO VISUAL STUDIO (CASO USE SSDT NO FUTURO)
###############################################################################
# É boa prática tratar arquivos de solução como binários para evitar conflitos de merge
*.sln merge=binary
*.suo merge=binary
*.user merge=binary